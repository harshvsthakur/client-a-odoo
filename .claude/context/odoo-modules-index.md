# Odoo modules index

One row per shipped module. This is the FIRST thing to check for "what already exists" -- read this before grepping addons/ or querying Notion Release Notes. Only fall back to a full grep/query if this index doesn't answer the question or seems out of date.

| Module | Model(s) touched | What it does | Ticket | Notion Release Note | Notes/quirks |
|---|---|---|---|---|---|
| custom_contact_notes | res.partner | Adds vip_notes text field, shown as tab | (pre-ticket) | (link) | |
| partner_priority_client | res.partner | Adds is_priority_client boolean, priority_level selection | (pre-ticket) | (link) | Required field on upgrade auto-backfills default |
| partner_urgency_flag | res.partner | Adds is_urgent boolean flag | TCK-2 | https://app.notion.com/p/Add-urgency-flag-to-Contacts-3948a366581581d58184fc01a1c44ed4 | |
| partner_contact_preference | res.partner | Adds preferred contact channel field (contact_preference selection) | TCK-3 | https://app.notion.com/p/Add-contact_preference-field-to-res-partner-3948a366581581538b26ebd7641c156a | Writing any res.partner field requires the Contact Creation group (base.group_partner_manager), not just base.group_user -- pre-existing, unrelated to this field (see TCK-4) |
| project_sale_order_ux | project.project | Reorders Projects app to default List view; adds a Project -> Sales Order smart button (reuses core sale_project's sale_order_id field, no new field) | TCK-5 | https://app.notion.com/p/Default-Projects-list-view-Project-Sales-Order-smart-button-3948a3665815816da270d57fe95c1136 | Button gated to sales_team.group_sale_salesman -- a plain project user without Sales access would otherwise hit an AccessError opening the linked sale.order |
| project_type_field | project.project | Adds project_type selection field (Permanent / SPAA) | TCK-6 | https://app.notion.com/p/Add-Project-Type-field-to-Projects-3948a366581581faa5e8f420715534d7 | required="1" in the form view only, NOT required=True at the model/DB level -- a hard DB-level required would break sale_project's automatic project creation on Sales Order confirmation and project_sale_order_ux's tests. Existing projects backfilled to "Permanent" via post_init_hook. |
| project_template | project.project, project.task, project.milestone, project.template (new) | New project.template model (task-list + milestone lines, day-offsets from project start). Adds project.project.template_id, required in the UI on project creation (detailed form + "Create a Project" dialog only, not the Kanban quick-add) but not DB-required. Creating a project with a template seeds project.task/project.milestone records from the template's lines. | TCK-7 | https://app.notion.com/p/User-defined-project-templates-3968a36658158144b7beceda0c7b16c0 | Adds project.task.planned_date_start/planned_date_end -- Community edition's project.task has no native "planned start" field. required="1" is view-only (same TCK-6 pattern) so sale_project's automatic project creation is unaffected. Kanban inline quick-add (name-only dialog) is a known, intentional gap -- not covered by the required-template UI gate. Deleting a template sets project.project.template_id to null (Odoo's default), doesn't cascade-delete projects. Applies going forward only -- no retroactive backfill for existing projects. |
| stock_store_production_route | stock.location (new: Store, Production, Production Consumption), stock.picking.type (new: Store to Production, Production Consume), product.category (new: Production Materials, AVCO + Automated valuation) | First Inventory-module customization. Data-only module (no Python models): creates Store and Production as internal sub-locations of WH/Stock, a "Store to Production" internal-transfer operation type (Pick = create, Store = validate -- one manual transfer, no automation), a "Production Consume" operation type moving stock from Production to a new "Production Consumption" virtual location, and a "Production Materials" product category set to AVCO costing + Automated/perpetual valuation wired to the generic COA's existing Stock Valuation/Interim accounts and Inventory Valuation journal. | TCK-8 | https://app.notion.com/p/Store-Production-inventory-route-with-returns-and-AVCO-valuation-3968a3665815818e99d9f4c66d3cac47 | Merged (PR #10, 2026-07-07). Manufacturing (mrp) is installed in this instance but idle (0 BOMs/MOs) -- deliberately not depended on or used; Consume is a plain internal transfer, not an MRP consumption. Odoo's own per-company "Virtual Locations/Production" (usage='production') location exists but has no stable external ID (created dynamically per company), so a dedicated "Production Consumption" location was created instead rather than reusing it. Returns use Odoo's native Return wizard on the Store-to-Production picking with no extra config -- verified it correctly generates the reverse Production-to-Store transfer using the same picking type. Verified end-to-end (Pick/Store/Consume/Return + COGS journal posting) both manually in `harsh-test` and via the automated test suite. Consumption moves now carry a project link (see TCK-10). |
| project_budget_recognition | project.project (new fields), stock.picking (new: project_id), account.account/account.journal (new: Unbilled Receivable/WIP, Recognized Revenue, Revenue Recognition journal) | Percentage-of-completion revenue recognition per project. Links POs to a project via the project's `account_id` (analytic account, auto-provisioned on project create under the core "Project" analytic plan) and PO lines' `analytic_distribution`. `committed_cost` = confirmed PO lines for service-type products only (excludes stockable goods to avoid double-counting with consumption); `consumption_cost` = valuation of stock moved into TCK-8's Production Consumption location, tagged via a new `project_id` field on `stock.picking` (required at validation for the Production Consume operation type, locked once state=done). `percent_complete = cost_incurred / total_budgeted_cost` (manual field, capped at 100%, blocked from being negative); `recognized_revenue = percent_complete * sale_order_id.amount_untaxed` (linked SO via TCK-5's `sale_line_id`/`sale_order_id`). Manual "Recognize Revenue" button on the Project form posts the incremental delta to a dedicated journal/accounts (debit Unbilled Receivable/WIP, credit Recognized Revenue), idempotent, posts a proper reversal (swapped debit/credit) on downward revision. | TCK-10 | https://app.notion.com/p/Budget-recognition-PO-committed-consumption-cost-revenue-recognition-39d8a366581581348116e87767a3d9f0 | Merged (PR #11, 2026-07-14). `sale_order_id` on project.project is a related/readonly field derived from `sale_line_id` (Sales Order Item) -- not directly settable; set `sale_line_id` instead. `purchase.order.line.analytic_distribution` is a JSON `{account_id_str: percent}` map, not a simple m2o -- must parse per line. QA found and fixed two bugs before PR: negative `total_budgeted_cost` produced a nonsensical negative `percent_complete` (now blocked by a constraint), and a validated Production Consume picking's `project_id` could be silently reassigned after the fact (now locked once `state == 'done'`). |
